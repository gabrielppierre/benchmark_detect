"""Dataset inspection utilities."""

from __future__ import annotations

import csv
import json
import re
import zipfile
from collections import Counter, defaultdict
from dataclasses import dataclass
from datetime import datetime
from pathlib import Path, PurePosixPath
from typing import Any
from xml.etree import ElementTree

from PIL import Image, UnidentifiedImageError

from towervision.utils.io import ensure_dir, read_yaml, write_text

IMAGE_EXTENSIONS = {".jpg", ".jpeg", ".png", ".bmp", ".tif", ".tiff", ".webp"}
ANNOTATION_EXTENSIONS = {".json", ".txt", ".xml", ".csv"}
TIMESTAMP_FORMAT = "%Y-%m-%d_%H-%M-%S"
FILENAME_PATTERN = re.compile(
    r"^(?P<prefix>[A-Za-z]+)_(?P<date>\d{8})(?P<time>\d{6})_(?P<seq>\d+)_?(?P<suffix>.*)$"
)


@dataclass(slots=True)
class SourceIndex:
    """Inventory of files in a dataset source."""

    path: Path
    kind: str
    files: list[str]
    file_sizes: dict[str, int]
    image_files: list[str]
    annotation_files: list[str]


@dataclass(slots=True)
class DatasetInspectionReport:
    """Structured dataset inspection summary."""

    source_path: str
    source_kind: str
    structure_tree: list[str]
    overview: str
    hypothesis: str
    inventory: dict[str, Any]
    annotation_format: dict[str, Any]
    quality: dict[str, Any]
    stats: dict[str, Any]
    grouping: dict[str, Any]
    uncertainties: list[str]


@dataclass(slots=True)
class InspectionArtifacts:
    """Paths generated by one dataset inspection run."""

    timestamp: str
    markdown_path: Path
    csv_path: Path
    latest_markdown_path: Path


def discover_default_dataset_path(project_root: Path) -> Path:
    """Choose a dataset source from the current repository."""

    params_path = project_root / "params.yaml"
    if params_path.exists():
        params = read_yaml(params_path, default={}) or {}
        dataset_root = params.get("paths", {}).get("raw_dataset_root")
        if dataset_root:
            resolved_root = project_root / dataset_root
            if resolved_root.exists():
                return resolved_root

    base_config_path = project_root / "configs" / "data" / "base.yaml"
    if base_config_path.exists():
        base_config = read_yaml(base_config_path, default={}) or {}
        dataset_root = base_config.get("dataset_root")
        if dataset_root:
            resolved_root = project_root / dataset_root
            if resolved_root.exists():
                return resolved_root

    raw_dir = project_root / "data" / "raw"
    if raw_dir.exists():
        raw_files = [path for path in raw_dir.rglob("*") if path.is_file()]
        if raw_files:
            return raw_dir

    zip_candidates = sorted(project_root.glob("*.zip"))
    if len(zip_candidates) == 1:
        return zip_candidates[0]
    if zip_candidates:
        return zip_candidates[0]

    raise FileNotFoundError("nenhum dataset detectado em data/raw ou em um .zip na raiz")


def inspect_dataset(dataset_path: Path) -> DatasetInspectionReport:
    """Inspect a dataset directory or zip archive."""

    source = _build_source_index(dataset_path)
    format_counts = Counter(
        _detect_annotation_file_format(source, annotation_file) for annotation_file in source.annotation_files
    )
    detected_formats = sorted(format_counts)

    structure_tree = _build_tree_summary(source)
    uncertainties: list[str] = []

    if "COCO" in detected_formats:
        coco_file = next(
            annotation_file
            for annotation_file in source.annotation_files
            if _detect_annotation_file_format(source, annotation_file) == "COCO"
        )
        coco_analysis = _analyze_coco(source, coco_file)
        overview = (
            f"Foram encontrados {coco_analysis['num_images']} arquivos de imagem e "
            f"{coco_analysis['num_annotations']} anotações em um pacote {source.kind}."
        )
        hypothesis = "O dataset atual parece ser um export COCO para detecção, com JSON único."
        uncertainties.extend(coco_analysis["uncertainties"])
        return DatasetInspectionReport(
            source_path=dataset_path.as_posix(),
            source_kind=source.kind,
            structure_tree=structure_tree,
            overview=overview,
            hypothesis=hypothesis,
            inventory=coco_analysis["inventory"],
            annotation_format=coco_analysis["annotation_format"],
            quality=coco_analysis["quality"],
            stats=coco_analysis["stats"],
            grouping=coco_analysis["grouping"],
            uncertainties=uncertainties,
        )

    generic_analysis = _analyze_generic(source)
    overview = (
        f"Foram encontrados {generic_analysis['inventory']['num_images']} arquivos de imagem e "
        f"{generic_analysis['inventory']['num_annotation_files']} arquivos de anotação."
    )
    hypothesis = "O formato atual não pôde ser confirmado com segurança além da inspeção superficial."
    uncertainties.extend(generic_analysis["uncertainties"])
    return DatasetInspectionReport(
        source_path=dataset_path.as_posix(),
        source_kind=source.kind,
        structure_tree=structure_tree,
        overview=overview,
        hypothesis=hypothesis,
        inventory=generic_analysis["inventory"],
        annotation_format=generic_analysis["annotation_format"],
        quality=generic_analysis["quality"],
        stats=generic_analysis["stats"],
        grouping=generic_analysis["grouping"],
        uncertainties=uncertainties,
    )


def generate_inspection_timestamp(now: datetime | None = None) -> str:
    """Generate a filesystem-safe timestamp for inspection outputs."""

    current = now or datetime.now()
    return current.strftime(TIMESTAMP_FORMAT)


def build_inspection_artifact_paths(
    project_root: Path,
    *,
    timestamp: str | None = None,
) -> InspectionArtifacts:
    """Build versioned output paths for one inspection run."""

    resolved_timestamp = timestamp or generate_inspection_timestamp()
    return InspectionArtifacts(
        timestamp=resolved_timestamp,
        markdown_path=(
            project_root
            / "docs"
            / "inspections"
            / "dataset"
            / resolved_timestamp
            / "dataset_current_state.md"
        ),
        csv_path=(
            project_root
            / "reports"
            / "tables"
            / "dataset"
            / resolved_timestamp
            / "dataset_summary.csv"
        ),
        latest_markdown_path=project_root / "docs" / "inspections" / "dataset" / "latest.md",
    )


def write_inspection_artifacts(
    project_root: Path,
    report: DatasetInspectionReport,
    *,
    timestamp: str | None = None,
) -> InspectionArtifacts:
    """Persist versioned markdown, CSV and a stable latest markdown."""

    artifacts = build_inspection_artifact_paths(project_root, timestamp=timestamp)
    markdown = render_markdown(report)
    write_text(artifacts.markdown_path, markdown)
    write_summary_csv(report, artifacts.csv_path)
    write_text(artifacts.latest_markdown_path, markdown)
    return artifacts


def render_markdown(report: DatasetInspectionReport) -> str:
    """Render a technical markdown summary for the current dataset state."""

    inventory = report.inventory
    annotation_format = report.annotation_format
    quality = report.quality
    stats = report.stats
    grouping = report.grouping

    class_names = inventory.get("classes", [])
    class_list = ", ".join(f"`{name}`" for name in class_names) if class_names else "não inferido"

    evidence_lines = "\n".join(f"- {item}" for item in annotation_format.get("evidence", []))
    consistency_checks = quality.get("checks", {})
    check_lines = "\n".join(
        f"- {title}: `{count}`" for title, count in consistency_checks.items()
    )
    issues = quality.get("issues", [])
    issue_lines = (
        "\n".join(
            f"- {issue['title']}: {issue['details']} Severidade aproximada: `{issue['severity']}`."
            for issue in issues
        )
        if issues
        else "- Nenhum problema estrutural relevante foi detectado na inspeção atual."
    )

    grouping_lines = "\n".join(f"- {item}" for item in grouping.get("group_candidates", []))
    limitation_lines = "\n".join(f"- {item}" for item in grouping.get("limitations", []))
    uncertainty_lines = "\n".join(f"- {item}" for item in report.uncertainties)

    bbox_stats = stats.get("bbox")
    if bbox_stats is None:
        bbox_block = "- Estatísticas de bbox não disponíveis para o formato detectado."
    else:
        bbox_block = "\n".join(
            [
                f"- largura: min `{bbox_stats['width_min']:.2f}`, max `{bbox_stats['width_max']:.2f}`, média `{bbox_stats['width_mean']:.2f}`",
                f"- altura: min `{bbox_stats['height_min']:.2f}`, max `{bbox_stats['height_max']:.2f}`, média `{bbox_stats['height_mean']:.2f}`",
                (
                    "- área relativa aproximada: "
                    f"min `{bbox_stats['relative_area_min']:.6f}`, "
                    f"max `{bbox_stats['relative_area_max']:.6f}`, "
                    f"média `{bbox_stats['relative_area_mean']:.6f}`"
                ),
            ]
        )

    class_count_lines = []
    for class_name, count in stats.get("class_counts", {}).items():
        extra = stats.get("bbox_by_class", {}).get(class_name)
        if extra is None:
            class_count_lines.append(f"- `{class_name}`: {count}")
            continue
        class_count_lines.append(
            f"- `{class_name}`: {count} anotações, área relativa média `{extra['relative_area_mean']:.6f}`"
        )
    class_count_block = "\n".join(class_count_lines) if class_count_lines else "- não inferido"

    return "\n".join(
        [
            "# Dataset Current State",
            "",
            "## 1. Visão geral",
            "",
            f"- {report.overview}",
            f"- Hipótese sobre o formato atual: {report.hypothesis}",
            "",
            "## 2. Estrutura atual",
            "",
            "```text",
            *report.structure_tree,
            "```",
            "",
            "## 3. Inventário",
            "",
            f"- número de imagens: `{inventory['num_images']}`",
            f"- número de anotações: `{inventory['num_annotations']}`",
            f"- número de arquivos de anotação: `{inventory['num_annotation_files']}`",
            f"- extensões de imagem: {', '.join(f'`{item}`' for item in inventory['image_extensions']) or 'nenhuma'}",
            f"- classes encontradas: {class_list}",
            "",
            "## 4. Formato das anotações",
            "",
            f"- formato aparente: `{annotation_format['primary']}`",
            "- evidências curtas:",
            evidence_lines,
            f"- observações importantes: {annotation_format['notes']}",
            "",
            "## 5. Qualidade e consistência",
            "",
            "- checks objetivos:",
            check_lines,
            "- observações relevantes:",
            issue_lines,
            "",
            "## 6. Estatísticas básicas",
            "",
            (
                "- resoluções: "
                f"min `{stats['images']['min_width']}x{stats['images']['min_height']}`, "
                f"max `{stats['images']['max_width']}x{stats['images']['max_height']}`, "
                f"média `{stats['images']['mean_width']:.2f}x{stats['images']['mean_height']:.2f}`"
            ),
            bbox_block,
            "- contagem por classe:",
            class_count_block,
            "",
            "## 7. Agrupamentos úteis para split",
            "",
            grouping_lines,
            "- limitações atuais:",
            limitation_lines,
            "",
            "## 8. Conclusão",
            "",
            f"- {quality['conclusion']}",
            f"- próximos passos recomendados: {quality['next_steps']}",
            *(
                ["", "## Incertezas", "", uncertainty_lines]
                if uncertainty_lines
                else []
            ),
        ]
    )


def write_summary_csv(report: DatasetInspectionReport, output_path: Path) -> Path:
    """Write a flat CSV summary of the inspection."""

    rows: list[dict[str, str]] = [
        {"section": "source", "metric": "path", "value": report.source_path},
        {"section": "source", "metric": "kind", "value": report.source_kind},
        {"section": "inventory", "metric": "num_images", "value": str(report.inventory["num_images"])},
        {
            "section": "inventory",
            "metric": "num_annotations",
            "value": str(report.inventory["num_annotations"]),
        },
        {
            "section": "inventory",
            "metric": "num_annotation_files",
            "value": str(report.inventory["num_annotation_files"]),
        },
        {
            "section": "inventory",
            "metric": "image_extensions",
            "value": "|".join(report.inventory["image_extensions"]),
        },
        {
            "section": "format",
            "metric": "primary",
            "value": str(report.annotation_format["primary"]),
        },
        {
            "section": "stats",
            "metric": "min_resolution",
            "value": f"{report.stats['images']['min_width']}x{report.stats['images']['min_height']}",
        },
        {
            "section": "stats",
            "metric": "max_resolution",
            "value": f"{report.stats['images']['max_width']}x{report.stats['images']['max_height']}",
        },
        {
            "section": "stats",
            "metric": "mean_resolution",
            "value": (
                f"{report.stats['images']['mean_width']:.2f}x"
                f"{report.stats['images']['mean_height']:.2f}"
            ),
        },
    ]

    bbox_stats = report.stats.get("bbox")
    if bbox_stats is not None:
        rows.extend(
            [
                {"section": "bbox", "metric": "width_mean", "value": f"{bbox_stats['width_mean']:.4f}"},
                {"section": "bbox", "metric": "height_mean", "value": f"{bbox_stats['height_mean']:.4f}"},
                {
                    "section": "bbox",
                    "metric": "relative_area_mean",
                    "value": f"{bbox_stats['relative_area_mean']:.6f}",
                },
            ]
        )

    for class_name, count in report.stats.get("class_counts", {}).items():
        rows.append(
            {
                "section": "class_counts",
                "metric": class_name,
                "value": str(count),
            }
        )

    ensure_dir(output_path.parent)
    with output_path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.DictWriter(handle, fieldnames=["section", "metric", "value"])
        writer.writeheader()
        writer.writerows(rows)
    return output_path


def render_terminal_summary(report: DatasetInspectionReport) -> str:
    """Render a concise executive summary for the terminal."""

    class_names = ", ".join(report.inventory.get("classes", [])) or "não inferido"
    issue_titles = ", ".join(issue["title"] for issue in report.quality.get("issues", [])) or "nenhum"
    return "\n".join(
        [
            "Dataset inspection summary",
            f"- source: {report.source_path} ({report.source_kind})",
            f"- images: {report.inventory['num_images']}",
            f"- annotation files: {report.inventory['num_annotation_files']}",
            f"- annotations: {report.inventory['num_annotations']}",
            f"- format: {report.annotation_format['primary']}",
            f"- classes: {class_names}",
            (
                "- resolutions: "
                f"{report.stats['images']['min_width']}x{report.stats['images']['min_height']} to "
                f"{report.stats['images']['max_width']}x{report.stats['images']['max_height']}"
            ),
            f"- quality flags: {issue_titles}",
        ]
    )


def _build_source_index(dataset_path: Path) -> SourceIndex:
    if dataset_path.is_dir():
        files = sorted(path.relative_to(dataset_path).as_posix() for path in dataset_path.rglob("*") if path.is_file())
        file_sizes = {
            path.relative_to(dataset_path).as_posix(): path.stat().st_size
            for path in dataset_path.rglob("*")
            if path.is_file()
        }
        kind = "directory"
    elif dataset_path.suffix.lower() == ".zip" and dataset_path.is_file():
        with zipfile.ZipFile(dataset_path) as archive:
            infos = [info for info in archive.infolist() if not info.is_dir()]
        files = sorted(info.filename for info in infos)
        file_sizes = {info.filename: info.file_size for info in infos}
        kind = "zip"
    else:
        raise FileNotFoundError(f"dataset source not supported: {dataset_path}")

    image_files = [file_path for file_path in files if PurePosixPath(file_path).suffix.lower() in IMAGE_EXTENSIONS]
    annotation_files = [
        file_path for file_path in files if PurePosixPath(file_path).suffix.lower() in ANNOTATION_EXTENSIONS
    ]
    return SourceIndex(
        path=dataset_path,
        kind=kind,
        files=files,
        file_sizes=file_sizes,
        image_files=image_files,
        annotation_files=annotation_files,
    )


def _detect_annotation_file_format(source: SourceIndex, file_path: str) -> str:
    suffix = PurePosixPath(file_path).suffix.lower()
    if suffix == ".json":
        payload = _read_source_json(source, file_path)
        if isinstance(payload, dict) and {"images", "annotations"}.issubset(payload):
            return "COCO"
        return "JSON customizado"
    if suffix == ".xml":
        try:
            root = ElementTree.fromstring(_read_source_bytes(source, file_path))
        except ElementTree.ParseError:
            return "XML"
        return "Pascal VOC" if root.tag == "annotation" else "XML"
    if suffix == ".csv":
        return "CSV"
    if suffix == ".txt":
        sample = _read_source_text(source, file_path)
        lines = [line.strip() for line in sample.splitlines() if line.strip()]
        if lines and all(_looks_like_yolo_line(line) for line in lines[:10]):
            return "YOLO"
        return "TXT"
    return "desconhecido"


def _analyze_coco(source: SourceIndex, annotation_file: str) -> dict[str, Any]:
    payload = _read_source_json(source, annotation_file)
    images = payload.get("images", [])
    annotations = payload.get("annotations", [])
    categories = payload.get("categories", [])

    image_by_id = {image["id"]: image for image in images}
    category_by_id = {category["id"]: str(category["name"]) for category in categories}
    class_names = [category_by_id[category["id"]] for category in categories]

    source_image_basenames = [PurePosixPath(file_path).name for file_path in source.image_files]
    source_basename_counter = Counter(source_image_basenames)
    image_name_counter = Counter(str(image.get("file_name", "")) for image in images)

    images_without_annotations = [
        str(image.get("file_name", f"id={image.get('id')}"))
        for image in images
        if image["id"] not in {annotation["image_id"] for annotation in annotations}
    ]
    annotations_without_image = [annotation["id"] for annotation in annotations if annotation["image_id"] not in image_by_id]

    images_missing_from_source: list[str] = []
    ambiguous_image_matches: list[str] = []
    for image in images:
        file_name = PurePosixPath(str(image.get("file_name", ""))).name
        count = source_basename_counter[file_name]
        if count == 0:
            images_missing_from_source.append(file_name)
        elif count > 1:
            ambiguous_image_matches.append(file_name)

    source_images_missing_from_metadata = [
        image_name for image_name, count in Counter(source_image_basenames).items() if image_name_counter[image_name] == 0
    ]

    duplicate_annotation_ids = [
        annotation_id
        for annotation_id, count in Counter(annotation["id"] for annotation in annotations).items()
        if count > 1
    ]
    duplicate_image_names = [
        image_name for image_name, count in image_name_counter.items() if count > 1
    ]
    duplicate_source_basenames = [
        image_name for image_name, count in source_basename_counter.items() if count > 1
    ]
    empty_files = [file_path for file_path, size in source.file_sizes.items() if size == 0]

    image_stats = _coco_image_stats(source, images)
    bbox_stats = _coco_bbox_stats(images, annotations, category_by_id)
    grouping = _infer_grouping_candidates(images, source.image_files)

    issues = [
        _issue("imagens sem anotação", len(images_without_annotations), "baixa", images_without_annotations[:5]),
        _issue("anotações sem imagem", len(annotations_without_image), "alta", annotations_without_image[:5]),
        _issue("arquivos vazios", len(empty_files), "média", empty_files[:5]),
        _issue(
            "imagens listadas no JSON mas ausentes no pacote",
            len(images_missing_from_source),
            "alta",
            images_missing_from_source[:5],
        ),
        _issue(
            "imagens presentes no pacote mas ausentes no JSON",
            len(source_images_missing_from_metadata),
            "média",
            source_images_missing_from_metadata[:5],
        ),
        _issue("bboxes inválidas", len(bbox_stats["invalid_bbox_examples"]), "alta", bbox_stats["invalid_bbox_examples"][:5]),
        _issue(
            "nomes ambíguos para casar JSON com imagens",
            len(ambiguous_image_matches),
            "média",
            ambiguous_image_matches[:5],
        ),
        _issue("duplicidade de annotation id", len(duplicate_annotation_ids), "alta", duplicate_annotation_ids[:5]),
        _issue("duplicidade de file_name no JSON", len(duplicate_image_names), "média", duplicate_image_names[:5]),
        _issue(
            "duplicidade de basename no pacote de imagens",
            len(duplicate_source_basenames),
            "média",
            duplicate_source_basenames[:5],
        ),
    ]
    filtered_issues = [issue for issue in issues if issue["count"] > 0]

    multi_class_note = (
        "O dataset não é de classe única: há categorias `torre` e `isoladores`, "
        "o que exige filtro explícito se o benchmark futuro focar só em isoladores."
    )
    if len(class_names) > 1:
        filtered_issues.append(
            {
                "title": "dataset multiclasse",
                "count": len(class_names),
                "severity": "média",
                "details": multi_class_note,
            }
        )

    conclusion = "O dataset está estruturalmente consistente e utilizável para diagnóstico, com formato COCO claro."
    next_steps = (
        "Fixar a política de filtro por classe, decidir se `torre` entra no treino e definir group_ids "
        "baseados em blocos temporais ou sequência de captura antes de criar splits."
    )

    uncertainties: list[str] = []
    if ambiguous_image_matches:
        uncertainties.append("Alguns file_names do JSON não casam de forma única com o pacote de imagens.")
    if not grouping["group_candidates"]:
        uncertainties.append("Não foi identificado um group_id forte a partir do nome dos arquivos.")
    if grouping["limitations"]:
        uncertainties.extend(grouping["limitations"])

    inventory = {
        "num_images": len(source.image_files),
        "num_annotations": len(annotations),
        "num_annotation_files": len(source.annotation_files),
        "image_extensions": sorted({PurePosixPath(file_path).suffix.lower() for file_path in source.image_files}),
        "classes": class_names,
    }
    annotation_format = {
        "primary": "COCO",
        "evidence": [
            f"arquivo de anotação `{annotation_file}`",
            "JSON com chaves `images`, `annotations` e `categories`",
            "anotações com campos `bbox`, `area`, `category_id` e `iscrowd`",
        ],
        "notes": (
            "O JSON também contém `segmentation` e `attributes`, mas o uso primário aparente é detecção "
            "com bounding boxes."
        ),
    }
    stats = {
        "images": image_stats,
        "bbox": bbox_stats["overall"],
        "class_counts": dict(bbox_stats["class_counts"]),
        "bbox_by_class": bbox_stats["by_class"],
    }
    quality = {
        "checks": {
            "imagens sem anotação": len(images_without_annotations),
            "anotações sem imagem": len(annotations_without_image),
            "arquivos vazios": len(empty_files),
            "bboxes inválidas": len(bbox_stats["invalid_bbox_examples"]),
            "nomes incompatíveis JSON->imagens": len(images_missing_from_source),
            "imagens extras fora do JSON": len(source_images_missing_from_metadata),
            "duplicidade de annotation id": len(duplicate_annotation_ids),
            "duplicidade de file_name no JSON": len(duplicate_image_names),
            "duplicidade de basename no pacote": len(duplicate_source_basenames),
        },
        "issues": filtered_issues,
        "conclusion": conclusion,
        "next_steps": next_steps,
    }
    return {
        "num_images": len(source.image_files),
        "num_annotations": len(annotations),
        "inventory": inventory,
        "annotation_format": annotation_format,
        "quality": quality,
        "stats": stats,
        "grouping": grouping,
        "uncertainties": uncertainties,
    }


def _analyze_generic(source: SourceIndex) -> dict[str, Any]:
    image_stats = _collect_image_stats(source, source.image_files)
    detected_formats = Counter(
        _detect_annotation_file_format(source, annotation_file) for annotation_file in source.annotation_files
    )
    primary_format = ", ".join(sorted(detected_formats)) if detected_formats else "desconhecido"
    return {
        "inventory": {
            "num_images": len(source.image_files),
            "num_annotations": 0,
            "num_annotation_files": len(source.annotation_files),
            "image_extensions": sorted({PurePosixPath(file_path).suffix.lower() for file_path in source.image_files}),
            "classes": [],
        },
        "annotation_format": {
            "primary": primary_format,
            "evidence": [f"formatos detectados por extensão: {dict(detected_formats)}"] if detected_formats else [],
            "notes": "Nenhuma análise profunda de bbox foi executada para o formato atual.",
        },
        "quality": {
            "checks": {
                "imagens sem anotação": 0,
                "anotações sem imagem": 0,
                "arquivos vazios": len([file_path for file_path, size in source.file_sizes.items() if size == 0]),
                "bboxes inválidas": 0,
                "nomes incompatíveis JSON->imagens": 0,
                "imagens extras fora do JSON": 0,
                "duplicidade de annotation id": 0,
                "duplicidade de file_name no JSON": 0,
                "duplicidade de basename no pacote": 0,
            },
            "issues": [],
            "conclusion": "A estrutura básica foi inventariada, mas o formato ainda precisa de análise específica.",
            "next_steps": "Adicionar parser específico para o formato predominante antes de qualquer conversão.",
        },
        "stats": {
            "images": image_stats,
            "bbox": None,
            "class_counts": {},
            "bbox_by_class": {},
        },
        "grouping": _infer_grouping_candidates_from_paths(source.image_files),
        "uncertainties": ["O formato atual não foi reconhecido como COCO, Pascal VOC, YOLO ou CSV de forma inequívoca."],
    }


def _coco_image_stats(source: SourceIndex, images: list[dict[str, Any]]) -> dict[str, float]:
    widths = [int(image["width"]) for image in images if image.get("width") is not None]
    heights = [int(image["height"]) for image in images if image.get("height") is not None]
    if len(widths) == len(images) and len(heights) == len(images):
        return {
            "min_width": min(widths),
            "min_height": min(heights),
            "max_width": max(widths),
            "max_height": max(heights),
            "mean_width": sum(widths) / len(widths),
            "mean_height": sum(heights) / len(heights),
        }
    return _collect_image_stats(source, source.image_files)


def _collect_image_stats(source: SourceIndex, image_files: list[str]) -> dict[str, float]:
    sizes: list[tuple[int, int]] = []
    if source.kind == "directory":
        for file_path in image_files:
            absolute_path = source.path / file_path
            try:
                with Image.open(absolute_path) as image:
                    sizes.append(image.size)
            except (UnidentifiedImageError, OSError):
                continue
    else:
        with zipfile.ZipFile(source.path) as archive:
            for file_path in image_files:
                try:
                    with archive.open(file_path) as handle:
                        with Image.open(handle) as image:
                            sizes.append(image.size)
                except (KeyError, UnidentifiedImageError, OSError):
                    continue

    if not sizes:
        return {
            "min_width": 0,
            "min_height": 0,
            "max_width": 0,
            "max_height": 0,
            "mean_width": 0.0,
            "mean_height": 0.0,
        }

    widths = [size[0] for size in sizes]
    heights = [size[1] for size in sizes]
    return {
        "min_width": min(widths),
        "min_height": min(heights),
        "max_width": max(widths),
        "max_height": max(heights),
        "mean_width": sum(widths) / len(widths),
        "mean_height": sum(heights) / len(heights),
    }


def _coco_bbox_stats(
    images: list[dict[str, Any]],
    annotations: list[dict[str, Any]],
    category_by_id: dict[int, str],
) -> dict[str, Any]:
    image_by_id = {image["id"]: image for image in images}
    widths: list[float] = []
    heights: list[float] = []
    relative_areas: list[float] = []
    invalid_bbox_examples: list[str] = []
    class_counts: Counter[str] = Counter()
    by_class: dict[str, dict[str, float]] = defaultdict(
        lambda: {"count": 0.0, "width_sum": 0.0, "height_sum": 0.0, "relative_area_sum": 0.0}
    )

    for annotation in annotations:
        bbox = annotation.get("bbox")
        if not isinstance(bbox, list) or len(bbox) != 4:
            invalid_bbox_examples.append(f"id={annotation.get('id')}: bbox ausente ou malformada")
            continue

        image = image_by_id.get(annotation["image_id"])
        if image is None:
            invalid_bbox_examples.append(f"id={annotation.get('id')}: image_id inexistente")
            continue

        x, y, width, height = (float(value) for value in bbox)
        reasons: list[str] = []
        if width <= 0 or height <= 0:
            reasons.append("largura/altura não positiva")
        if x < 0 or y < 0:
            reasons.append("origem negativa")
        if x + width > float(image["width"]):
            reasons.append("ultrapassa largura da imagem")
        if y + height > float(image["height"]):
            reasons.append("ultrapassa altura da imagem")
        if reasons:
            invalid_bbox_examples.append(f"id={annotation.get('id')}: {', '.join(reasons)}")
            continue

        widths.append(width)
        heights.append(height)
        relative_area = (width * height) / (float(image["width"]) * float(image["height"]))
        relative_areas.append(relative_area)

        class_name = category_by_id.get(annotation["category_id"], str(annotation["category_id"]))
        class_counts[class_name] += 1
        by_class[class_name]["count"] += 1
        by_class[class_name]["width_sum"] += width
        by_class[class_name]["height_sum"] += height
        by_class[class_name]["relative_area_sum"] += relative_area

    if widths:
        overall = {
            "width_min": min(widths),
            "width_max": max(widths),
            "width_mean": sum(widths) / len(widths),
            "height_min": min(heights),
            "height_max": max(heights),
            "height_mean": sum(heights) / len(heights),
            "relative_area_min": min(relative_areas),
            "relative_area_max": max(relative_areas),
            "relative_area_mean": sum(relative_areas) / len(relative_areas),
        }
    else:
        overall = None

    bbox_by_class: dict[str, dict[str, float]] = {}
    for class_name, values in by_class.items():
        count = values["count"] or 1.0
        bbox_by_class[class_name] = {
            "count": values["count"],
            "width_mean": values["width_sum"] / count,
            "height_mean": values["height_sum"] / count,
            "relative_area_mean": values["relative_area_sum"] / count,
        }

    return {
        "overall": overall,
        "class_counts": class_counts,
        "by_class": bbox_by_class,
        "invalid_bbox_examples": invalid_bbox_examples,
    }


def _infer_grouping_candidates(
    images: list[dict[str, Any]],
    source_image_files: list[str],
) -> dict[str, list[str]]:
    file_names = [PurePosixPath(str(image.get("file_name", ""))).name for image in images]
    folder_counter = Counter(str(PurePosixPath(file_path).parent) for file_path in source_image_files)

    prefix_counter: Counter[str] = Counter()
    date_counter: Counter[str] = Counter()
    minute_counter: Counter[str] = Counter()
    suffix_counter: Counter[str] = Counter()
    sequences: list[int] = []
    unmatched_names: list[str] = []

    for file_name in file_names:
        stem = Path(file_name).stem
        match = FILENAME_PATTERN.match(stem)
        if match is None:
            unmatched_names.append(file_name)
            continue
        prefix_counter[match.group("prefix")] += 1
        date_counter[match.group("date")] += 1
        minute_counter[f"{match.group('date')}{match.group('time')[:4]}"] += 1
        suffix_counter[match.group("suffix") or "<none>"] += 1
        sequences.append(int(match.group("seq")))

    candidates: list[str] = []
    limitations: list[str] = []

    if len(folder_counter) > 1:
        candidates.append(f"pasta de origem: {dict(folder_counter)}")
    elif folder_counter:
        limitations.append(
            f"todas as imagens estão sob a mesma pasta de origem observável: {next(iter(folder_counter))}"
        )

    if len(date_counter) > 1:
        candidates.append(f"data no nome do arquivo: {dict(date_counter)}")
    elif date_counter:
        limitations.append(
            f"a data aparente no nome é única para todo o conjunto: {next(iter(date_counter))}"
        )

    if len(minute_counter) > 1:
        candidates.append(f"blocos temporais por minuto no nome: {dict(minute_counter)}")
    else:
        limitations.append("não há variação suficiente de minuto no nome para definir blocos temporais distintos")

    if len(prefix_counter) > 1:
        candidates.append(f"prefixo de captura: {dict(prefix_counter)}")
    elif prefix_counter:
        limitations.append(f"o prefixo de captura é único: {next(iter(prefix_counter))}")

    if len(suffix_counter) > 1:
        candidates.append(f"sufixo no nome do arquivo: {dict(suffix_counter)}")
    elif suffix_counter:
        limitations.append(f"o sufixo no nome do arquivo é único: {next(iter(suffix_counter))}")

    if sequences:
        contiguous = len(set(sequences)) == (max(sequences) - min(sequences) + 1)
        if contiguous:
            candidates.append(
                f"sequência contínua de captura no nome: {min(sequences):04d}-{max(sequences):04d}"
            )
        else:
            limitations.append("a sequência numérica dos arquivos não é contínua")

    if unmatched_names:
        limitations.append(
            f"{len(unmatched_names)} arquivos não seguiram o padrão nominal esperado; exemplos: {unmatched_names[:5]}"
        )

    return {"group_candidates": candidates, "limitations": limitations}


def _infer_grouping_candidates_from_paths(image_files: list[str]) -> dict[str, list[str]]:
    folder_counter = Counter(str(PurePosixPath(file_path).parent) for file_path in image_files)
    candidates = [f"pasta de origem: {dict(folder_counter)}"] if len(folder_counter) > 1 else []
    limitations = []
    if len(folder_counter) <= 1:
        limitations.append("as imagens parecem vir de uma única pasta observável")
    return {"group_candidates": candidates, "limitations": limitations}


def _build_tree_summary(source: SourceIndex, max_depth: int = 3) -> list[str]:
    file_paths = [PurePosixPath(file_path) for file_path in source.files]
    if not file_paths:
        return [f"{source.path.name}/"]

    root_name = source.path.name
    leading_parts = {path.parts[0] for path in file_paths if path.parts}
    if len(leading_parts) == 1:
        root_name = next(iter(leading_parts))
        trimmed_paths = [PurePosixPath(*path.parts[1:]) for path in file_paths if len(path.parts) > 1]
    else:
        trimmed_paths = file_paths

    directory_counts: Counter[tuple[str, ...]] = Counter()
    for path in trimmed_paths:
        parts = path.parts[:-1]
        for depth in range(1, min(len(parts), max_depth) + 1):
            directory_counts[parts[:depth]] += 1

    lines = [f"{root_name}/"]

    def render(prefix: tuple[str, ...], indent: str) -> None:
        children = sorted(
            child[-1]
            for child in directory_counts
            if child[:-1] == prefix and len(child) == len(prefix) + 1
        )
        for index, child_name in enumerate(children):
            connector = "└──" if index == len(children) - 1 else "├──"
            child_key = prefix + (child_name,)
            file_count = directory_counts[child_key]
            lines.append(f"{indent}{connector} {child_name}/ ({file_count} arquivos)")
            if len(child_key) < max_depth:
                next_indent = f"{indent}{'    ' if index == len(children) - 1 else '│   '}"
                render(child_key, next_indent)

    render((), "")
    return lines


def _looks_like_yolo_line(line: str) -> bool:
    parts = line.split()
    if len(parts) < 5:
        return False
    try:
        float(parts[0])
        float(parts[1])
        float(parts[2])
        float(parts[3])
        float(parts[4])
    except ValueError:
        return False
    return True


def _read_source_bytes(source: SourceIndex, file_path: str) -> bytes:
    if source.kind == "directory":
        return (source.path / file_path).read_bytes()
    with zipfile.ZipFile(source.path) as archive:
        return archive.read(file_path)


def _read_source_text(source: SourceIndex, file_path: str) -> str:
    return _read_source_bytes(source, file_path).decode("utf-8", errors="replace")


def _read_source_json(source: SourceIndex, file_path: str) -> Any:
    return json.loads(_read_source_text(source, file_path))


def _issue(title: str, count: int, severity: str, samples: list[Any]) -> dict[str, Any]:
    sample_text = f" Exemplos: {samples}." if samples else ""
    return {
        "title": title,
        "count": count,
        "severity": severity,
        "details": f"{count} ocorrência(s).{sample_text}",
    }
